import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.markerJsTest',
  appName: 'markerjs-test',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
